# Changelog

## [1.1.0] - 2026-08-20

弹幕烧录版。弹幕可以直接烧进画面输出单个 MP4，安装包同时缩小到原来的三分之一。

### 新增
- **弹幕烧录**：弹幕渲染进视频像素，产出单个自带弹幕的 MP4，任何播放器、任何平台都能看到，
  也不再有播放器实时渲染弹幕的卡顿。优先使用手机硬件编码器（实测约 9~11 倍实时速度），
  失败自动回退软件编码。合并前可选「快速合并 / 弹幕烧录」。
- **弹幕筛选两档**：「精选弹幕」只保留彩色、顶部/底部与 B 站权重最高档的弹幕，并全部静止显示；
  「全部弹幕」再加回普通滚动弹幕。外挂 `.ass` 与烧录都遵循此设置。
- **同款弹幕去重**：内容完全相同的弹幕只保留最早一条。
- **列表勾选**：扫描结果可逐项勾选，默认全选，支持全选/全不选。
- **中断导出**：导出中点击按钮即可结束，半成品文件自动删除，未处理项标记为已中断。
- **通知栏进度**：烧录走前台服务并显示进度，切到后台不会被系统冻结。

### 变更
- 安装包 **44 MB → 15.7 MB**。自行交叉编译精简版 FFmpeg（45 MB → 9.3 MB，并去掉 8.8 MB 的
  `libc++_shared.so` 依赖），同时剔除 APK 中多余的 armeabi-v7a / x86_64 引擎。
  构建脚本与补丁见 `tools/ffmpeg/`。
- 弹幕默认参数调整为字号 50、速度 1.25x、透明度 0.8。
- **移除「渲染画质」设置**：它并不影响清晰度，libass 始终会按比例缩放到视频实际分辨率，
  该选项实为变相的字号调节器，容易误导。弹幕大小现在由字号一项控制，坐标系固定 1080P。
- 新增 `THIRD-PARTY.md` 说明随应用分发的 FFmpeg 及其依赖的许可证。

### 修复
- 弹幕宽度估算不再把中英文一视同仁（中文原先被低估约 20%，导致防重叠实际失效）。
- 滚动弹幕改为统一速度，消除长弹幕追上并穿过短弹幕的问题。
- 顶部/底部弹幕改为真正分配轨道，此前它们会全部叠在同一行。
- 彩色弹幕的颜色标签改为规范写法，修复其透明度与白色弹幕不一致的问题。
- 弹幕正文中的 `{` `}` `\` 不再破坏整行 ASS。
- 防重叠在显示区域接近全屏时会失效（顶部末行与底部首行重叠，并有文字越出画面底边），
  现改为顶部与底部共用一份轨道预算，并使用整数行距。已在字号 24~100 × 显示区域 0.2~1.0
  的全部组合上验证零重叠、零越界。
- 修复中断在「弹幕转换阶段」被吞掉、烧录仍继续的竞态。
- 修复前台服务在极快失败时通知栏残留、以及后台启动前台服务导致崩溃的问题。
- 修复模式选择弹窗弹出期间可重复点击、并发跑起两轮导出的问题。
- 弹幕总开关关闭时不再允许烧录。

## [1.0.0] - 2026-07-11

首个正式版本。将 B 站移动端缓存的 `.m4s` 音视频流合并为完整 MP4，并支持弹幕转换。

### 功能
- **智能扫描**：识别 B 站缓存目录，读取 `entry.json` 标题与分集信息。
- **无损合并**：内置 FFmpeg 以 stream-copy（`-c copy`）方式合并音视频轨道，不重编码。
- **弹幕转换**：XML → ASS，可调透明度、字体、大小、速度、显示区域与渲染画质（720P/1080P/2K/4K）。
- **防重叠布局**：滚动弹幕按轨道空闲情况智能分配，避免同屏叠字。
- **个性化主题**：粉/蓝/紫/绿多种配色。
- **自定义输出**：自由选择合并后视频的保存路径。

### 关键修复
- 修复多 P / 合集视频因共用顶层标题导致输出文件互相覆盖的问题（改为标题 + 分集名，并做唯一性去重）。
- 合并超时由 30 秒放宽至 30 分钟，避免大文件被误判超时中断。
- 移除无实际作用的冗余弹幕设置项，清理死代码与未用依赖。
